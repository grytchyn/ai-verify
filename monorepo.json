{
  "name": "ai-verify",
  "version": "2.0.0",
  "description": "AI-Verify v2.0 — Full-stack AI Compliance Platform (Unicorn Scale)",
  "workspaces": [
    "frontend",
    "backend",
    "ai-engine",
    "infrastructure"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "test": "jest",
    "lint": "eslint .",
    "db:migrate": "npx prisma migrate dev",
    "db:push": "npx prisma db push",
    "ai:deploy": "cd ai-engine && ./deploy.sh",
    "infrastructure:deploy": "cd infrastructure/terraform && terraform init && terraform apply"
  }
}
